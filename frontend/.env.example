# ========================================================
# Synthetic Minds Frontend - Environment Configuration
# ========================================================

# Optional: Set when frontend is deployed on Vercel separately from backend API
# If left empty, API requests use relative paths (/api) handled by Vercel rewrites or local proxy
# VITE_API_BASE_URL=https://api.yourdomain.com
